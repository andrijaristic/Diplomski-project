export enum ApiCallState {
  PENDING,
  COMPLETED,
  REJECTED,
}

export enum CommentLocation {
  ACCOUNT,
  DETAILED_PAGE,
}

export enum UserType {
  RENTEE,
  OWNER,
}

export enum HookTypes {
  TEXT,
  REGEX,
}
